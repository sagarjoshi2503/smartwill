# Base URL of the smartwill-api FastAPI backend this MCP server wraps.
API_BASE_URL=http://127.0.0.1:8051

# Host/port the MCP server itself listens on (streamable-HTTP transport).
MCP_HOST=0.0.0.0
MCP_PORT=8000
